guests = ["Alice", "Kirito", "Asuna", "Yuki","suguha"]
print("Only three people can be invited.")

while len(guests) > 3:
    removed = guests.pop()# remove the last guest from the list.
    print(f"Sorry {removed}, you can't come.")

for guest in guests:
    print(f"{guest}, you're still invited.")
