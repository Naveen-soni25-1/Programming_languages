def make_shirt(size="L", message="I love Python"): # function with default values.
    """prints the size and message of the shirt."""
    print(f"Making a {size}-sized shirt with message: '{message}'")

make_shirt()
make_shirt("M", "Code is Life")
